#include "holberton.h"
/**
 * _isdigit - check the code for Holberton School students.
 * @c: variable
 * Return: 1 if c is uppercase and 0 if otherwise.
 */
int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		return (1);
	}
	return (0);
}
